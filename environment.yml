name: dms
channels:
  - anaconda
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - click
  - python=3.7
  - pip
  - pytorch
  - scipy
  - seaborn
  - matplotlib
  - pip:
      - black
      - click-config-file
      - dms_variants
      - docformatter
      - flake8
      - pylint
      - pytest
